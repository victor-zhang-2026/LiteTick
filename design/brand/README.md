# 妥了 LiteTick brand assets

- `litetick-app-icon.svg` — production square app-icon layer; macOS owns the final mask, shadow, and highlight.
- `litetick-alert-icon.svg` — borderless rounded presentation for About and quit confirmation.

The public brand is “妥了 LiteTick”. The authored white outer ring belongs only to the floating desktop trigger and is not part of the application icon or other logo presentations.
